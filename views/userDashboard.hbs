<h1> Hello {{user.name}}! </h1>

<div id="card">
  {{#each post}}
  <div id="card">
    <img src="{{thumbnail}}"/>
    <h2>{{title}}</h2>
    <small>{{category}}</small>
  </div>
  <button><a href="/auth/{{_id}}">Details</a></button>
  <br/>
  {{/each}}
</div>