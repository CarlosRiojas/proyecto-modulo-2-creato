<h1> Hello {{name}}! </h1>

<div>
  {{#each post}}
  <div>
    <img src="{{thimbnail}}"/>
    <h2>{{title}}</h2>
    <small>{{category}}</small>
  </div>
  <button><a href="/auth/{{_id}}">Details</a></button>
  <br/>
  {{/each}}
</div>